/* General Styles */
body {
    font-family: 'Lora', serif; /* Cambia la fuente por una serif elegante */
    background-color: #F5F5DC; /* Fondo beige claro */
    color: #333; /* Texto oscuro para buen contraste */
    margin: 0;
    padding: 0;
    line-height: 1.6;
}

header {
    background-color: #D2B48C; /* Beige oscuro */
    color: #FFF;
    padding: 20px 0;
    text-align: center;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

h1 {
    font-size: 2.5em;
    margin: 0;
    font-weight: bold;
    color: #FFFFFF; /* Blanco */
}

h2 {
    font-size: 2em;
    color: #8B4513; /* Marrón */
    border-bottom: 2px solid #8B4513;
    padding-bottom: 10px;
    margin-bottom: 20px;
}

section {
    margin: 20px auto;
    padding: 20px;
    background-color: #FFF8DC; /* Fondo beige claro */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    max-width: 800px;
    border-radius: 8px;
}

p, li {
    font-size: 1.1em;
    color: #4B4B4B; /* Gris oscuro */
}

ul {
    list-style-type: square;
    padding-left: 20px;
}

ul li {
    margin: 10px 0;
}

form {
    margin-top: 20px;
}

textarea {
    width: 100%;
    padding: 12px;
    font-size: 1em;
    border: 1px solid #CCC;
    border-radius: 4px;
    box-sizing: border-box;
    margin-bottom: 10px;
    resize: vertical;
    background-color: #FAF0E6; /* Beige pálido */
    color: #333;
}

button {
    background-color: #8B4513; /* Marrón */
    color: #FFF;
    padding: 12px 24px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 1em;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #A0522D; /* Marrón claro */
}

/* Slideshow Styles */
.slideshow-container {
    position: relative;
    max-width: 1000px;
    margin: auto;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.mySlides {
    display: none;
}

img {
    vertical-align: middle;
    border-radius: 8px;
}

.text {
    color: #FFF;
    font-size: 1.5em;
    padding: 12px;
    position: absolute;
    bottom: 8px;
    width: 100%;
    text-align: center;
    background-color: rgba(0, 0, 0, 0.5);
    border-radius: 0 0 8px 8px;
}

/* Buttons for previous/next */
.prev, .next {
    cursor: pointer;
    position: absolute;
    top: 50%;
    width: auto;
    padding: 16px;
    margin-top: -22px;
    color: #FFF;
    font-weight: bold;
    font-size: 18px;
    transition: 0.6s ease;
    border-radius: 0 3px 3px 0;
    user-select: none;
    background-color: rgba(0, 0, 0, 0.4);
}

.next {
    right: 0;
    border-radius: 3px 0 0 3px;
}

.prev:hover, .next:hover {
    background-color: rgba(0, 0, 0, 0.8);
}

/* Footer Styles */
footer {
    background-color: #D2B48C; /* Beige oscuro */
    color: #FFF;
    text-align: center;
    padding: 20px 0;
    margin-top: 20px;
    box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.1);
}

footer p {
    margin: 0;
    font-size: 1em;
    color: #FFF;
}
